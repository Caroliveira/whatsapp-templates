export enum ComponentParameterEnum {
  Image = "image",
  Video = "video",
  Document = "document",
  Text = "text",
}

export enum ComponentTypeEnum {
  Header = "header",
  Body = "body",
  Footer = "footer",
  Button = "button",
}
